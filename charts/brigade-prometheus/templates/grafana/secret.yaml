apiVersion: v1
kind: Secret
metadata:
  name: {{ include "brigade-prometheus.grafana.fullname" . }}
  labels:
    {{- include "brigade-prometheus.labels" . | nindent 4 }}
    {{- include "brigade-prometheus.grafana.labels" . | nindent 4 }}
type: Opaque
stringData:
  auth-hash: {{ htpasswd .Values.grafana.auth.proxyUsername .Values.grafana.auth.proxyPassword }}
